rules:
  - id: prepend-module-wins
    languages: [ruby]
    severity: ERROR
    message: A prepended module comes before the class in the resolution order.
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
