rules:
  - id: test-prefixed-import
    languages: [dart]
    message: A prefixed call reaches the library the prefix names, not the other one
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
