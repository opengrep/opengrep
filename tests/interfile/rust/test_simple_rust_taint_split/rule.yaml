rules:
  - id: simple_rust_taint
    options:
      taint_interfile: true
    message: Simple Rust cross-function taint
    languages:
      - rust
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)