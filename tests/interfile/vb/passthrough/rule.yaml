rules:
  - id: test-passthrough
    languages: [vb]
    message: Tainted data flows through a multi-file passthrough chain
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: Source()
    pattern-sinks:
      - pattern: Sink($X)
