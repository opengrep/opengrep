class Tainted_user:
    def __init__(self, seller):
        self.key = source()
    def props(self):
        __init__(self, taint)
    # ruleid: python_constructor_sqli
        query = f"SELECT * FROM table WHERE name = {self.key}"
        return query

