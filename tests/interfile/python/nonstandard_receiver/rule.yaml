rules:
  - id: nonstandard-receiver
    options:
      taint_interfile: true
    message: taint reaches sink through a method with a non-standard receiver name
    languages: [python]
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
