rules:
  - id: cpp_constructor_sqli
    options:
      taint_interfile: true
    message: SQL injection through constructor
    languages:
      - cpp
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)