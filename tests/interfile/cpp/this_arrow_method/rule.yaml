rules:
  - id: this-arrow-method
    languages: [cpp]
    message: a call through a pointer and a call through this bind to the method of the receiver's class
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
