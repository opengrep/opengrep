rules:
  - id: using-declaration
    languages: [cpp]
    message: a using declaration binds one name from one namespace, not the homonym of a second namespace
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
