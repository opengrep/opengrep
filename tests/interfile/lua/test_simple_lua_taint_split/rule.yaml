rules:
  - id: simple_lua_taint
    options:
      taint_interfile: true
    message: Simple Lua cross-function taint
    languages:
      - lua
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)