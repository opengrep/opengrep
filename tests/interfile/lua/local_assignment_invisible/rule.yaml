rules:
  - id: local_assignment_invisible
    languages: [lua]
    severity: ERROR
    message: probe
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
