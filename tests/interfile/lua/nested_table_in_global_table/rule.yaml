rules:
  - id: nested_table_in_global_table
    languages: [lua]
    message: probe
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
