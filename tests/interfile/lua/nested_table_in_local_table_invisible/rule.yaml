rules:
  - id: nested_table_in_local_table_invisible
    languages: [lua]
    message: probe
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
