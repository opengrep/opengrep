rules:
  - id: require_of_file_returning_nothing
    languages: [lua]
    message: probe
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
