rules:
  - id: class-path-identity
    languages: [crystal]
    severity: ERROR
    message: A class written with its constant path is the class of that path, not a same named top level class
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
