rules:
  - id: test-import-only-binds-named
    languages: [elixir]
    message: Tainted data flows only through the functions an import names
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
