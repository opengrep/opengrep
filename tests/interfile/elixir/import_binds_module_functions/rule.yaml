rules:
  - id: test-import-binds-module-functions
    languages: [elixir]
    message: Tainted data flows through a function made local by an import
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
