rules:
  - id: test-alias-binds-last-segment
    languages: [elixir]
    message: Tainted data flows through the module bound by the last alias segment
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
