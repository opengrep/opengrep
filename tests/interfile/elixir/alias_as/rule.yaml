rules:
  - id: test-alias-as
    languages: [elixir]
    message: Tainted data flows through the module bound by an alias with as
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
