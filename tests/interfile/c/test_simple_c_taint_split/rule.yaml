rules:
  - id: simple_c_taint
    options:
      taint_interfile: true
    message: Simple C taint test
    languages:
      - c
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
        focus-metavariable: $X