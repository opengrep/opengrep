rules:
  - id: test-project-scope-two-files
    languages: [julia]
    message: A bare call reaches the one function of that name in the project's module
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
