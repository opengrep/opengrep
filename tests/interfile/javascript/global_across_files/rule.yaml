rules:
  - id: global-across-files-js
    languages: [javascript]
    severity: ERROR
    message: a write to a module variable reaches the functions that call the writer
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
