rules:
  - id: nested-namespace-binds-type
    languages: [csharp]
    message: A using directive of a nested namespace binds the types declared in it.
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
