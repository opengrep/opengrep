rules:
  - id: test-hiding-import
    languages: [scala]
    message: A hidden name is not bound by the wildcard that follows it
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
