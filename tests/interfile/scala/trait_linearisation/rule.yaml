rules:
  - id: test-trait-linearisation
    languages: [scala]
    message: The last mixin wins the linearisation
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
