rules:
  - id: test-package-binding
    languages: [scala]
    message: A bare object name resolves in the caller's own package
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
