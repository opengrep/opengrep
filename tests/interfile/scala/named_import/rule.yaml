rules:
  - id: test-named-import
    languages: [scala]
    message: A named import binds one object of two with the same name
    severity: ERROR
    mode: taint
    options:
      taint_interfile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
