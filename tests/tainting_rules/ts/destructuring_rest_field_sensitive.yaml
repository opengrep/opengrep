rules:
  - id: rest-field-sensitive
    languages: [typescript]
    severity: ERROR
    message: taint reached sink
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
