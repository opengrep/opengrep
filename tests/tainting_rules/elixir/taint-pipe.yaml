rules:
- id: pipe_taint
  languages: [elixir]
  message: "tainted data reached sink via pipe"
  severity: ERROR
  mode: taint
  pattern-sources:
    - patterns:
      - pattern-either:
          - pattern-inside: |
              def $_(..., $X, ...) do
              ...
              end
      - focus-metavariable: $X
  pattern-sinks:
    - pattern: |
        sink(...)
