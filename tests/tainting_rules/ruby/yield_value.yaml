rules:
  - id: yield_value
    message: taint through the value of yield
    languages:
      - ruby
    severity: WARNING
    mode: taint
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
