rules:
- id: taint
  languages: [go]
  message: "tainted receiver reached sink"
  severity: ERROR
  mode: taint
  pattern-sources:
    - patterns:
      - pattern: |
          func ($RECV *SomeType) $METHOD(...) { ... }
      - focus-metavariable: $RECV
  pattern-sinks:
    - pattern: sink(...)
