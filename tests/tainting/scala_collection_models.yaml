rules:
  - id: scala-collection-taint
    mode: taint
    languages: [scala]
    message: "Taint flows through Scala collection to sink"
    severity: WARNING
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: tainted
    pattern-sinks:
      - pattern: sink(...)
