rules:
  - id: ruby_heredoc_dots_in_target
    message: three dots in the text of a target heredoc are text, not the ellipsis
    languages:
      - ruby
    severity: WARNING
    pattern: foo("=~/^ *[.]{3}[[:space:]]+$/")
