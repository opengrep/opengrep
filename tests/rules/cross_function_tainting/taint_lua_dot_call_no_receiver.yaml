rules:
  - id: taint_lua_dot_call_no_receiver
    message: Lua taint through a colon call
    languages:
      - lua
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)
