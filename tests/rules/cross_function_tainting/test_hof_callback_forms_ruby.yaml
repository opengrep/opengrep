rules:
  - id: test-hof-callback-forms-ruby
    message: Ruby HOF callback forms (lambda, Proc.new, method ref)
    languages:
      - ruby
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
