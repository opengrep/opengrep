rules:
  - id: test-guards-composite-cond
    message: Guard whose condition embeds a composite must survive rebinding
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
