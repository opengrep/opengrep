rules:
  - id: test-interprocedural-dart
    message: Interprocedural taint flow detected from source to sink
    languages:
      - dart
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
