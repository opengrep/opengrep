rules:
  - id: taint_lua_self_argument
    message: Lua taint through a colon call
    languages:
      - lua
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)
