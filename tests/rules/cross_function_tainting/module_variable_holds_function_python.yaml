rules:
  - id: module_variable_holds_function_python
    message: a call through a module variable reaches every function assigned to it
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
