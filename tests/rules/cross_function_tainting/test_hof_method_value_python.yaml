rules:
  - id: test-hof-method-value-python
    message: a method value of an untyped receiver passed as a callback
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
