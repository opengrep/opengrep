rules:
  - id: test-capture-modes-cpp
    message: a closure holds a variable captured by value as it was at creation
    languages:
      - cpp
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink(...)
