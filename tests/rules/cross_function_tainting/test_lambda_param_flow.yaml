rules:
  - id: test-lambda-param-flow
    message: Tainted data flows to sink via lambda parameter
    languages:
      - go
    severity: WARNING
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - pattern: sink($X)
