class PaginatedUtmLinksPresenter:
    def __init__(self, seller):
        self.key = taint
    def props(self):
        __init__(self, taint)
    # ruleid: test
        query = f"SELECT * FROM table WHERE id = {self.key}"
        return query

