rules:
  - id: kotlin_constructor_sqli
    message: SQL injection through constructor
    languages:
      - kotlin
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)
