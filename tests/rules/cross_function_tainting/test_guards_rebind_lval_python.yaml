rules:
  - id: test-guards-rebind-lval
    message: ToLval guard rebinding across a call chain
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
      effect_guards: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
