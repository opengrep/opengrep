rules:
  - id: taint_lua_same_field_two_tables
    message: A lua dotted call resolves to the function defined at that name
    languages:
      - lua
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)
