rules:
  - id: hack_rooted_name_in_namespace_block
    message: A rooted Hack name inside a namespace block calls the function of the global namespace
    languages:
      - hack
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)
