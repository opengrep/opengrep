rules:
  - id: test-hof-callback-forms-python
    message: Python HOF callback that is a lambda assigned to a variable (positive vs non-propagating body)
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
