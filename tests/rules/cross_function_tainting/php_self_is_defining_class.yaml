rules:
  - id: php_self_is_defining_class
    message: "PHP self:: calls the method of the class where the call is written; static:: dispatches on the class of the call"
    languages:
      - php
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)
