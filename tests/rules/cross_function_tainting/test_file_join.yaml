rules:
  - id: test-file-join
    message: taint
    languages: [ruby]
    severity: ERROR
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: File.$Z(...)
          - metavariable-regex:
              metavariable: $Z
              regex: ^join$
    pattern-sinks:
      - pattern: sink($EXPR)
