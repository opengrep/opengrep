rules:
  - id: test-effect-guards-off
    message: Guards are inert without the effect_guards option
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
