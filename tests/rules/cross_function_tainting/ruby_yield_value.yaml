rules:
  - id: ruby_yield_value
    message: taint through the value of yield
    languages:
      - ruby
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
