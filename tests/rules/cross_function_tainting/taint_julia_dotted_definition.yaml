rules:
  - id: taint-julia-dotted-definition
    message: A julia function defined at a field is called through that field
    languages:
      - julia
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
    pattern-sinks:
      - patterns:
          - pattern: sink($X)
