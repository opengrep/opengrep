rules:
  - id: test-hof-taint
    message: Taint should flow through multi-clause Elixir functions
    languages:
      - elixir
    severity: WARNING
    mode: taint
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
