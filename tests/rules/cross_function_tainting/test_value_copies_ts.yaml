rules:
  - id: test-value-copies-ts
    message: An object type is not a value type
    languages:
      - ts
    severity: WARNING
    mode: taint
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
