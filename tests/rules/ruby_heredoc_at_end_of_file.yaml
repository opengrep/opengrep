rules:
  - id: ruby_heredoc_at_end_of_file
    message: a heredoc that ends a file with no trailing newline keeps its body
    languages:
      - ruby
    severity: WARNING
    pattern: run("=~/DELETE/")
